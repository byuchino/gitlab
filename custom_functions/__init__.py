def __init__(**kwargs):
    """
    Init helper for Global_Util. Effectively turns our helper directory into a proper python module
    
    Returns a JSON-serializable object that implements the configured data paths:
        
    """
    ############################ Custom Code Goes Below This Line #################################
